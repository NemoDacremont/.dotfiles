#include <iostream>
#define ll long long

using namespace std;

void solve(void)
{
}

int main(int argc, char* argv[])
{
	ios::sync_with_stdio(false);
	cin.tie(nullptr), cout.tie(nullptr);
	int t; cin>>t;
	for (int i = 0 ; i < t ; ++i)
		solve();
	return 0;
}

